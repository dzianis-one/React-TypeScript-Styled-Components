import * as React from 'react';

import { HeaderTitle, HeaderStyled } from './styles';

const Header = () => {
  return <HeaderStyled>
      <HeaderTitle>
        Hello
      </HeaderTitle>
  </HeaderStyled>;
};

export default Header;
